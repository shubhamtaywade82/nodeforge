# @nodeforge/adapter-devdocs

Downloads [DevDocs.io](https://devdocs.io) docsets from `https://downloads.devdocs.io/{slug}.tar.gz`, extracts them to a cache directory, and supports offline full-text search over `db.json`.

Used by the NodeForge extension global storage (`devdocs/`). Not imported by `vscode` packages.
